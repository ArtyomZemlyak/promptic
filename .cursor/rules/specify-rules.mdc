# promptic Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-11-17

## Active Technologies
- Python 3.11 (CPython) + `pydantic>=2`, `pydantic-settings`, `rich` (preview rendering), `jinja2` (templating), `orjson` (JSON serialization), `pytest`, `pytest-asyncio`, `hypothesis` (property-based testing), optional adapter extras (`httpx`, `sqlalchemy`, `faiss-cpu`) (001-context-engine)
- Filesystem-backed instruction stores (Markdown/JSON/YAML/plain-text) with optional caching (LRU); data/memory adapters abstract storage (CSV, HTTP, vector DBs, etc.) (001-context-engine)
- Python ≥3.11 (per `pyproject.toml`) + Promptic core modules (pydantic, jinja2, rich, orjson, yaml) plus filesystem I/O (003-file-first-prompts)
- File-based blueprints/instructions/memory folders (no new DB) (003-file-first-prompts)
- Python 3.11 (CPython) + `pydantic>=2`, `pydantic-settings`, `jinja2` (templating), `pyyaml>=6.0` (YAML parsing), `orjson` (JSON serialization), `tiktoken` (token counting), `rich` (preview rendering), `pytest`, `pytest-asyncio`, `hypothesis` (property-based testing) (004-unified-context-node)
- Filesystem-backed node storage (YAML, Markdown, Jinja2, JSON files) with optional caching; in-memory nodes supported for programmatic creation (004-unified-context-node)
- Python 3.11 (CPython) + `pydantic>=2`, `pydantic-settings`, existing promptic dependencies (`rich`, `jinja2`, `orjson`), `pytest`, `pytest-asyncio`, `packaging` (for semantic versioning comparison), `structlog` or standard `logging` (for structured logging) (005-prompt-versioning)
- Filesystem-based version detection and export operations. Version information stored in file naming conventions (postfixes like `_v1.0.0`, `_v1`, `_v1.1`). Export creates directory structures preserving hierarchical layout. No additional storage requirements beyond filesystem. (005-prompt-versioning)

- Python 3.11 (CPython) + `pydantic>=2`, `pydantic-settings`, `typer` (CLI), `rich` (logging/preview), `jinja2` (templating), `orjson`, optional adapter extras (e.g., `httpx`, `sqlalchemy`, `faiss-cpu`) (001-context-engine)

## Project Structure

```text
src/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python 3.11 (CPython): Follow standard conventions

## Recent Changes
- 005-prompt-versioning: Added Python 3.11 (CPython) + `pydantic>=2`, `pydantic-settings`, existing promptic dependencies (`rich`, `jinja2`, `orjson`), `pytest`, `pytest-asyncio`, `packaging` (for semantic versioning comparison), `structlog` or standard `logging` (for structured logging)
- 004-unified-context-node: Added Python 3.11 (CPython) + `pydantic>=2`, `pydantic-settings`, `jinja2` (templating), `pyyaml>=6.0` (YAML parsing), `orjson` (JSON serialization), `tiktoken` (token counting), `rich` (preview rendering), `pytest`, `pytest-asyncio`, `hypothesis` (property-based testing)
- 003-file-first-prompts: Added Python ≥3.11 (per `pyproject.toml`) + Promptic core modules (pydantic, jinja2, rich, orjson, yaml) plus filesystem I/O


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
