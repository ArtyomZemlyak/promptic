[pytest]
minversion = 7.0
addopts = -ra
testpaths = tests
markers =
    unit: Unit tests for individual components.
    integration: Integration tests spanning multiple layers.
    contract: Contract/interface tests verifying public SDK surfaces.
