# Example 8: Export with Variables

This example demonstrates how to use variable insertion when exporting a prompt hierarchy to a file-first structure.

## Features

- Exporting versioned prompts to a directory
- Applying variables during export
- Hierarchical path scoping in export mode
