## Step-by-step Instructions

1. Load the file using `render()`
2. Pass any variables needed
3. Get the rendered output
4. Use the output in your application

That's it! No need to load networks separately.
