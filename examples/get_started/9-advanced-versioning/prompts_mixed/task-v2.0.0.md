# Task Processor v2.0.0 (Mixed - Hyphen)

This is version 2.0.0 using hyphen delimiter.

## Overview

In mixed directories, files can use different naming conventions.
This file uses hyphen: `task-v2.0.0.md`

Both delimiters work together when `delimiters=["_", "-"]` is configured.
