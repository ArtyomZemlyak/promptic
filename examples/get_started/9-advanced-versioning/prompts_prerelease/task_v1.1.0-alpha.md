# Task Processor v1.1.0-alpha

This is an ALPHA prerelease of version 1.1.0.

## Overview

Alpha releases are early development versions for testing new features.

Prerelease ordering: `alpha < beta < rc < release`

This version will NOT be selected by `version="latest"` unless
`include_prerelease=True` is configured.
