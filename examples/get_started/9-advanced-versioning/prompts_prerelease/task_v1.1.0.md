# Task Processor v1.1.0

This is the stable release version 1.1.0.

## Overview

This is the stable release that follows all the prereleases:
- v1.1.0-alpha
- v1.1.0-beta
- v1.1.0-rc.1
- v1.1.0 (this version - STABLE)

When using `version="latest"` with default settings (include_prerelease=False),
this version will be selected as the latest stable release.
