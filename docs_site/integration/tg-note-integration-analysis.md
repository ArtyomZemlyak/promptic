# Интеграция promptic в tg-note: Анализ возможностей

**Дата**: 2025-01-27  
**Проект**: tg-note ([GitHub](https://github.com/ArtyomZemlyak/tg-note))  
**Библиотека**: promptic (file-first context engineering)

## 📋 Обзор tg-note

**tg-note** — Telegram бот для создания заметок в GitHub knowledge base с использованием AI агентов:
- Использует **qwen code cli** для обработки запросов
- Обрабатывает медиа-файлы (OCR картинок в `.md` файлы)
- Управляет knowledge base через Git
- Промпты хранятся в `config/prompts/`

## 🎯 Проблемы, которые решает promptic

### Текущая ситуация в tg-note

1. **Большой контекст промптов**: Все инструкции передаются в qwen code cli сразу, что:
   - Занимает много токенов
   - Ограничивает гибкость агента
   - Усложняет версионирование промптов

2. **Жёсткая структура**: Промпты в `config/prompts/` вероятно:
   - Монолитные файлы со всеми инструкциями
   - Сложно изменять и тестировать
   - Нет иерархии и ссылок между инструкциями

3. **OCR и медиа-обработка**: Судя по README, есть логика:
   ```markdown
   Если хочешь узнать подробности, OCR или транскрипцию конкретного медиа,
   то заменяй файловое расширение в названии файла медиа на .md и читай этот файл.
   ```
   Это уже file-first подход, но не структурированный.

## 💡 Как promptic может помочь

### 1. File-First рендеринг промптов

**Текущий подход (предположительно):**
```python
# В tg-note, вероятно, так:
prompt = load_prompt("config/prompts/note_creation.md")  # Весь файл целиком
response = qwen_code_cli.run(prompt)
```

**С promptic (file-first):**
```python
from promptic import load_blueprint, render_for_llm

# Загружаем blueprint с file-first режимом
blueprint = load_blueprint("config/prompts/note_creation.yaml")
# Рендерим только краткий промпт с ссылками
compact_prompt = render_for_llm(blueprint, render_mode="file_first")

# qwen code cli получает компактный промпт:
# "Ты ассистент. Инструкции:
# - Создавай структурированные заметки
# - Используй Markdown форматирование
#
# Шаги:
# 1. Проанализировать сообщение (подробнее - instructions/analyze.md)
# 2. Создать заметку (подробнее - instructions/create_note.md)
# 3. Обработать медиа (подробнее - instructions/media.md)
#
# Если нужно OCR медиа, читай файл: media/{filename}.md"
```

**Преимущества:**
- ✅ Контекст уменьшается на 60-80% (только краткие инструкции + ссылки)
- ✅ Агент сам решает, какие инструкции читать
- ✅ Легко добавлять новые инструкции без изменения основного промпта

### 2. Иерархическая структура промптов

**Структура с promptic:**

```
config/prompts/
├── note_creation.yaml          # Основной blueprint
├── instructions/
│   ├── analyze.md              # Анализ сообщения
│   ├── create_note.md          # Создание заметки
│   ├── media.md                # Обработка медиа
│   │   └── ocr_instructions.md # Детали OCR (вложенная инструкция)
│   └── git_operations.md       # Git операции
└── memory/
    └── format.md               # Формат памяти
```

**Пример blueprint (`note_creation.yaml`):**
```yaml
name: Note Creation Workflow
prompt_template: |
  Ты ассистент для создания заметок в GitHub knowledge base.

  Основные правила:
  - Создавай структурированные заметки в Markdown
  - Используй Git для версионирования
  - Обрабатывай медиа через OCR когда нужно

steps:
  - step_id: analyze
    title: Анализ сообщения
    instruction_refs:
      - instruction_id: analyze
        path: instructions/analyze.md

  - step_id: create
    title: Создание заметки
    instruction_refs:
      - instruction_id: create_note
        path: instructions/create_note.md

  - step_id: media
    title: Обработка медиа
    instruction_refs:
      - instruction_id: media
        path: instructions/media.md

memory_channels:
  - name: knowledge_base
    format_path: memory/format.md
```

### 3. Интеграция с OCR workflow

**Текущий подход в tg-note:**
Агент должен сам понимать, что для OCR нужно читать `media/{filename}.md`

**С promptic (структурированно):**
```markdown
<!-- instructions/media.md -->
# Обработка медиа-файлов

## Список медиа для обработки:
{{ media_list }}

## Инструкции по OCR:

Если нужно получить OCR или транскрипцию конкретного медиа:
1. Замени расширение файла на `.md`
2. Прочитай файл: `media/{filename}.md`
3. Там будет OCR результат или инструкции

## Формат результата:
- Изображения → OCR текст в `media/{filename}.md`
- Аудио → Транскрипция в `media/{filename}.md`
- Видео → Транскрипция + ключевые кадры
```

**Blueprint может ссылаться на эту инструкцию:**
```yaml
steps:
  - step_id: media
    instruction_refs:
      - instruction_id: media
        path: instructions/media.md
    # Агент сам решит, когда читать OCR файлы
```

### 4. Версионирование промптов

**Проблема в tg-note:**
- Промпты меняются, но нет версионирования
- Сложно откатиться к предыдущей версии
- Нет истории изменений

**С promptic:**
```python
from promptic import load_blueprint

# Загрузить последнюю версию
blueprint = load_blueprint("note_creation")

# Загрузить конкретную версию
blueprint_v1 = load_blueprint("note_creation", version="1.2.0")

# Регистр промптов (будущая фича)
registry = PromptRegistry()
registry.register(blueprint, version="1.3.0")
```

### 5. Унифицированный формат (ContextNode)

**После реализации unified ContextNode архитектуры:**

```yaml
# config/prompts/note_creation.yaml
nodes:
  - type: blueprint
    content: |
      Ты ассистент для создания заметок...
    references:
      - path: instructions/analyze.md
      - path: instructions/create_note.md
      - path: instructions/media.md
      - path: memory/format.md
```

**Или в Markdown:**
```markdown
# Note Creation Workflow

Ты ассистент для создания заметок...

## Шаги:
1. [Анализ сообщения](instructions/analyze.md)
2. [Создание заметки](instructions/create_note.md)
3. [Обработка медиа](instructions/media.md)

## Память:
- Формат: [memory/format.md](memory/format.md)
```

**Преимущества:**
- ✅ Любой формат (YAML, Markdown, Jinja2, JSON)
- ✅ Рекурсивные ссылки между нодами
- ✅ Динамическая смена семантики (инструкция → данные → память)

## 🔌 Точки интеграции

### 1. Замена загрузки промптов

**Было:**
```python
# src/tg_note/agents/qwen_code_cli.py
def load_prompt(self, prompt_name: str) -> str:
    with open(f"config/prompts/{prompt_name}.md") as f:
        return f.read()
```

**Стало:**
```python
from promptic import load_blueprint, render_for_llm

def load_prompt(self, prompt_name: str, render_mode: str = "file_first") -> str:
    blueprint = load_blueprint(f"config/prompts/{prompt_name}")
    return render_for_llm(blueprint, render_mode=render_mode)
```

### 2. Интеграция с qwen code cli

```python
# src/tg_note/agents/qwen_code_cli.py
from promptic import load_blueprint, render_for_llm

class QwenCodeCliAgent:
    def __init__(self):
        self.blueprint = load_blueprint("config/prompts/note_creation")

    async def process_message(self, message: str) -> str:
        # Рендерим компактный промпт с file-first подходом
        prompt = render_for_llm(
            self.blueprint,
            render_mode="file_first",
            base_url="https://github.com/user/kb/tree/main"  # Для абсолютных ссылок
        )

        # qwen code cli получает компактный промпт
        # Агент сам решает, какие инструкции читать
        response = await self.qwen_cli.run(prompt, message)
        return response
```

### 3. Управление инструкциями для OCR

```python
# src/tg_note/processors/media.py
from promptic import load_blueprint, render_for_llm

class MediaProcessor:
    def __init__(self):
        # Загружаем blueprint для обработки медиа
        self.media_blueprint = load_blueprint("config/prompts/media_processing")

    async def process_image(self, image_path: str) -> str:
        # OCR через qwen code cli с file-first промптом
        prompt = render_for_llm(
            self.media_blueprint,
            render_mode="file_first",
            context={"image_path": image_path}
        )

        # Агент получает инструкцию:
        # "Обработай изображение {image_path}
        # Если нужно OCR, читай: media/{filename}.md"

        ocr_result = await self.qwen_cli.run(prompt)

        # Сохраняем OCR результат для будущих ссылок
        ocr_file = f"media/{Path(image_path).stem}.md"
        with open(ocr_file, "w") as f:
            f.write(ocr_result)

        return ocr_result
```

## 📊 Сравнение: До и После

| Аспект | Без promptic | С promptic |
|--------|-------------|------------|
| **Размер контекста** | Весь промпт целиком (5000+ токенов) | Краткий промпт + ссылки (1000-2000 токенов) |
| **Гибкость** | Жёсткая структура, сложно менять | Иерархия файлов, легко добавлять инструкции |
| **Версионирование** | Нет | Встроенное (будущая фича) |
| **Форматы** | Только Markdown | YAML, Markdown, Jinja2, JSON |
| **OCR интеграция** | Ручная логика в коде | Структурированные ссылки в инструкциях |
| **Тестирование** | Сложно тестировать промпты | Легко тестировать отдельные инструкции |

## 🚀 План внедрения

### Этап 1: Базовая интеграция (MVP)
1. Установить promptic в tg-note
2. Конвертировать один промпт в blueprint формат
3. Заменить загрузку промпта на `render_for_llm` с file-first режимом
4. Протестировать с qwen code cli

### Этап 2: Структурирование промптов
1. Разбить монолитные промпты на иерархию инструкций
2. Создать blueprint файлы для каждого workflow
3. Настроить ссылки между инструкциями
4. Интегрировать с OCR workflow

### Этап 3: Расширенная функциональность
1. Использовать unified ContextNode архитектуру (после реализации)
2. Добавить версионирование промптов
3. Настроить регистр промптов
4. Оптимизировать контекст для разных сценариев

## ⚠️ Важные замечания

1. **qwen code cli должен поддерживать file-first подход**: Агент должен уметь читать файлы по ссылкам из промпта. Если нет — нужно добавить эту функциональность.

2. **Git интеграция**: Файлы инструкций должны быть в Git, чтобы агент мог их читать. Возможно, нужен адаптер для чтения из GitHub.

3. **Кэширование**: promptic поддерживает кэширование инструкций, что полезно для частых запросов.

4. **Обратная совместимость**: Старые промпты должны продолжать работать через адаптеры.

## 📝 Примеры использования

### Пример 1: Создание заметки

**Blueprint:**
```yaml
# config/prompts/note_creation.yaml
name: Note Creation
prompt_template: |
  Ты ассистент для создания заметок в GitHub knowledge base.

  Шаги:
  1. Проанализировать сообщение (подробнее - instructions/analyze.md)
  2. Создать структурированную заметку (подробнее - instructions/create_note.md)
  3. Обработать медиа если есть (подробнее - instructions/media.md)

  Формат памяти: memory/format.md
```

**Рендер (file-first):**
```markdown
Ты ассистент для создания заметок в GitHub knowledge base.

Шаги:
1. Проанализировать сообщение (подробнее - instructions/analyze.md)
2. Создать структурированную заметку (подробнее - instructions/create_note.md)
3. Обработать медиа если есть (подробнее - instructions/media.md)

Формат памяти: memory/format.md
```

**Агент получает компактный промпт и сам решает, какие инструкции читать.**

### Пример 2: OCR обработка

**Blueprint:**
```yaml
# config/prompts/media_processing.yaml
name: Media Processing
prompt_template: |
  Обработай медиа-файл: {{ image_path }}

  Инструкции:
  - Если нужно OCR, читай файл: media/{{ filename }}.md
  - Если файл не существует, создай его через OCR
  - Сохрани результат в Markdown формате
```

**Инструкция для OCR:**
```markdown
# instructions/ocr.md

## Процесс OCR:

1. Загрузи изображение
2. Выполни OCR через qwen code cli
3. Сохрани результат в `media/{filename}.md`
4. Верни структурированный текст

## Формат результата:
- Заголовок из изображения
- Основной текст
- Списки и таблицы если есть
```

## 🎯 Выводы

**promptic может помочь tg-note:**

1. ✅ **Уменьшить контекст** — передавать только краткие промпты с ссылками
2. ✅ **Структурировать промпты** — иерархия файлов вместо монолитных промптов
3. ✅ **Упростить OCR workflow** — структурированные ссылки на OCR файлы
4. ✅ **Версионирование** — управление версиями промптов (будущая фича)
5. ✅ **Гибкость форматов** — YAML, Markdown, Jinja2, JSON (после unified ContextNode)

**Главное преимущество:** qwen code cli получает компактный промпт и сам решает, какие инструкции читать, что идеально соответствует file-first подходу promptic.
